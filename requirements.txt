# ══════════════════════════════════════════════════════════════
# Grivora AI — Production dependencies
# Tested with Python 3.10 / 3.11 on Windows, Linux, macOS
# ══════════════════════════════════════════════════════════════

# ── Core web framework ───────────────────────────────────────
flask==3.0.0
python-dotenv==1.0.1
gunicorn==21.2.0
werkzeug>=3.0.0,<4.0.0

# ── Data processing ──────────────────────────────────────────
pandas>=2.1.0,<2.3.0
numpy>=1.26.0,<2.0.0

# ── Universal file loader support ────────────────────────────
chardet>=5.2.0                  # CSV encoding auto-detection
openpyxl>=3.1.2                 # .xlsx
xlrd>=2.0.1                     # .xls (legacy Excel)
pyxlsb>=1.0.10                  # .xlsb (binary Excel)
odfpy>=1.4.1                    # .ods (OpenDocument)
pyarrow>=14.0.0                 # .parquet

# ── Machine learning core ────────────────────────────────────
scikit-learn>=1.3.0,<1.6.0
scipy>=1.11.0

# ── ML optional boosters (PMA engine guards against missing) ─
xgboost>=2.0.0
lightgbm>=4.1.0
catboost>=1.2.0                  # gradient boosting, best for categorical-heavy data
statsmodels>=0.14.0             # ARIMA, SARIMA, Holt-Winters
prophet>=1.1.5                  # Facebook Prophet forecasting
hdbscan>=0.8.33                 # density-based clustering (handles noise)
shap>=0.44.0                    # model explainability (used in Sprint C)

# ── Visualization helpers ────────────────────────────────────
matplotlib>=3.8.0
plotly>=5.17.0

# ── AI / LLM ─────────────────────────────────────────────────
# NOTE: google-generativeai is now legacy (EOL Nov 2025, bug-fix only).
# Migration to google-genai is planned — tracked in Sprint 2.
google-generativeai>=0.8.5,<0.9.0

# ── PDF / report generation ──────────────────────────────────
reportlab>=4.0.0

# ── Database (optional — BIA MySQL path) ─────────────────────
mysql-connector-python>=8.2.0
SQLAlchemy>=2.0.0,<3.0.0

# ── Testing ──────────────────────────────────────────────────
pytest>=7.4.0

# ── Security / rate limiting ─────────────────────────────────
Flask-Limiter>=3.5.0             # LLM endpoint throttling
Flask-WTF>=1.2.0                 # CSRF protection
